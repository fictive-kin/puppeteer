# Manifest: classes/base
#
# Base class that all nodes/roles should include.
#
# This manifest should only define the very bare minimums that every node/role
# should have to be considered functional.
